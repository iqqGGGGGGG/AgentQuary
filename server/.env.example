AI_API_KEY=your-mimo-api-key-here

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=agentquary

# WeChat Mini Program (leave empty for dev mode)
WX_APPID=
WX_SECRET=

# Dev mode: use code as openid directly (no WeChat API call)
DEV_MODE=true

# Required outside dev mode. Generate at least 32 random bytes and keep it secret.
AUTH_SECRET=
AUTH_TOKEN_TTL_SECONDS=604800
